# chat-server
Created simple chatserver using socket io and CRA (create-react-app)


To run the file: 

`yarn run install`

To start the application:

`yarn run build`
`yarn run start` 

OR 

`yarn run dev`
